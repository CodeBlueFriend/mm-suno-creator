# 故障排除

- 查询为空：保留原词并登记为待核验，不要编造；尝试中英文别名或更短关键词。
- Style 冲突未识别：词典只识别已登记规范词，先查询标签再提交新冲突关系。
- 时长不准：加入完整段落与歌词；器乐段、拖腔和生成模型会扩大误差。
- 脚本找不到数据：必须从完整 Skill 目录运行，确保 `assets/tags.json` 存在。
- Windows 显示乱码：使用 UTF-8 终端并设置 `PYTHONUTF8=1`。
