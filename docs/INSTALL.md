# 安装

需要 Python 3.10+；查询与校验脚本没有第三方依赖。macOS、Windows 和 Linux 均可运行。

将 `skills/mm-suno-creator` 整个目录复制到 Codex Skills 目录；macOS 默认是 `~/.codex/skills/mm-suno-creator`。重新打开任务后，以 `$mm-suno-creator` 显式调用即可。离线状态下除平台规则核验外全部功能可用。

卸载时删除复制后的 Skill 目录即可。本项目不下载模型、不建立全局缓存，也不访问用户的 Suno 账号。
